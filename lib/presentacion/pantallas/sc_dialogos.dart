import 'package:flutter/material.dart';

class PantallaDialogos extends StatelessWidget{
  const PantallaDialogos({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Dialogos de la pantalla'),
      ),
      body: const Placeholder(),
    );
  }
}